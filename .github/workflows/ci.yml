name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  backend:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: inventory_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        working-directory: backend
        run: |
          pip install -r requirements.txt
      - name: Run migrations
        working-directory: backend
        env:
          DATABASE_URL: postgresql+asyncpg://postgres:postgres@localhost:5432/inventory_test
          DATABASE_URL_SYNC: postgresql+psycopg2://postgres:postgres@localhost:5432/inventory_test
          SECRET_KEY: ci-test-secret-key-minimum-32-characters
          API_V1_PREFIX: /api/v1
        run: alembic upgrade head
      - name: Pytest
        working-directory: backend
        env:
          DATABASE_URL: postgresql+asyncpg://postgres:postgres@localhost:5432/inventory_test
          DATABASE_URL_SYNC: postgresql+psycopg2://postgres:postgres@localhost:5432/inventory_test
          SECRET_KEY: ci-test-secret-key-minimum-32-characters
          API_V1_PREFIX: /api/v1
        run: pytest -q

  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install and test
        working-directory: frontend
        run: |
          npm install
          npm run test
          npm run build
